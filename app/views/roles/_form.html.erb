<div class="card">
  <%= form_with(model: role) do |form| %>
    <% if role.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(role.errors.count, "error") %> prohibited this role from being saved:</h2>

        <ul>
          <% role.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div class="field">
      <%= form.text_field :name, placeholder: "Name", :class => "login-input" %>
    </div>

    <div class="field">
      <%= form.text_field :description, placeholder: "Name", :class => "login-input" %>
    </div>

    <div class="actions">
      <%= form.submit nil, :class => 'btn-default btn-success' %>
    </div>
  <% end %>
</div>
